MyProPlanner stable V.4.1

Created: 2026-04-03

Purpose:
- Locked stable snapshot after fixing the blank report print flow, adding Excel `.xlsx` export, making Gantt task bars shade from lighter to darker based on completion, and simplifying the planner toolbar/sidebar layout.
- Use the matching archive `snapshots/MyProPlanner-stable-V.4.1.tgz` as the rollback backup.

Notes:
- Frontend and backend builds passed when this snapshot was created.
- Archive excludes generated folders like `frontend/node_modules`, `frontend/dist`, `backend/node_modules`, `backend/dist`, `.git`, and the `snapshots` folder itself.
