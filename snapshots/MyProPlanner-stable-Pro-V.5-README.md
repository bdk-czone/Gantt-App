MyProPlanner stable Pro V.5

Created: 2026-04-03

Purpose:
- Locked stable snapshot after adding passkey protection and session-based access, project resource links/uploads with preview support, and shared workload pages for external progress sharing.
- Use the matching archive `snapshots/MyProPlanner-stable-Pro-V.5.tgz` as the rollback backup.

Notes:
- Frontend and backend builds passed when this snapshot was created.
- Archive excludes generated folders like `frontend/node_modules`, `frontend/dist`, `backend/node_modules`, `backend/dist`, `.git`, and the `snapshots` folder itself.
