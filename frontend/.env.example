# Optional.
# Leave empty to use same-origin `/api` requests, which is recommended for deployed share links.
# For local development you can also leave this empty because Vite proxies `/api` to `http://localhost:3001`.
VITE_API_URL=
